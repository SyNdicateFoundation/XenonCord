package ir.xenoncommunity.gui.components;

import javax.swing.*;

public class MainGui extends JFrame {
    public MainGui(){
        setTitle("XenonCord Proxy");
        setSize(800, 400);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLayout(null);
        setVisible(true);
    }
}